# Paginas MVP antigas (Tailwind)

Substituidas pelo novo frontend (`landing.html` + `dashboard.html`).

Mantidas para referencia e logica JS ainda ligada a controllers:

- `app.html` — area utilizador (tabs + chatbot funcional)
- `login.html` / `register.html` — auth localStorage

Entrada actual do projecto: `landing.html` ou `index.html`.
