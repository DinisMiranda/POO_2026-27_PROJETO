{
  "name": "zenify",
  "private": true,
  "type": "module",
  "scripts": {
    "web": "serve . -l 5000",
    "data": "json-server-auth db.json --port 3000 --host 0.0.0.0 --watch",
    "seed-db": "node scripts/seed-db-users.js",
    "chat-api": "node server/chat-api.js",
    "api": "json-server-auth db.json --port 3000 --host 0.0.0.0",
    "start": "json-server-auth db.json --port 3000 --host 0.0.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0",
    "serve": "^14.2.4"
  }
}
